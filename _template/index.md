# Page title

*(One-line description of what this page is.)*

Copy this folder to start a new page:

```
cp -r _template <new-page-name>
```

Then rename this heading, write your content below, drop any images in this
folder (or a subfolder), and add a line for the page to `pages/index.md`.

## Section

Your content here. Keep everything the page needs inside this folder so it stays
self-contained.
